<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chokchey.flywaydemo.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.chokchey.flywaydemo.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
    </resultMap>

    <select id="findAll" resultMap="UserResultMap">
        SELECT * FROM users ORDER BY id DESC
    </select>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, phone)
        VALUES (#{username}, #{email}, #{phone})
    </insert>

    <update id="update">
        UPDATE users
        SET username = #{username},
            email = #{email},
            phone = #{phone}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <select id="searchByUsername" resultMap="UserResultMap">
        SELECT * FROM users
        WHERE username ILIKE CONCAT('%', #{username}, '%')
        ORDER BY id DESC
    </select>

</mapper>
